# Google Cultural Alignment (GCA)
A Python-based agent identifying high-potential talent via Enneagram and Google core values.

## Run
1. `source venv/bin/activate`
2. `pip install -r requirements.txt`
3. `uvicorn app.main:app --reload`